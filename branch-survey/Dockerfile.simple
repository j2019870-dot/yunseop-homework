FROM node:20-alpine

WORKDIR /app

# package.json 복사 및 의존성 설치
COPY package*.json ./
RUN npm install --only=production

# 모든 파일 복사
COPY . .

# Next.js 빌드
RUN npm run build

# 포트 설정
ENV PORT=8080
ENV NODE_ENV=production

EXPOSE 8080

# 애플리케이션 시작
CMD ["npm", "start"]
